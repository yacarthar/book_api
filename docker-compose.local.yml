services:
  app:
    build: .
    container_name: fast_api
    ports:
      - "8000:8000"

    env_file:
      - .local.env

    volumes:
      - .:/app

    working_dir: /app

    command: uvicorn app.main:app --host 0.0.0.0 --port 8000

    restart: unless-stopped